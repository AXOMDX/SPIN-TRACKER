import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    host: true,
    proxy: {
      '/api': 'http://localhost:3000',
      '/socket.io': 'http://localhost:3000'
    }
  },
  preview: {
    host: true,
    port: 4173,
    allowedHosts: [
      'spintracker-frontend.onrender.com',
      '.onrender.com',
      'localhost',
      '127.0.0.1'
    ]
  }
});
